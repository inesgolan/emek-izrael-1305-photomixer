<Window x:Class="photomixerGUI.endScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:photomixerGUI"
        mc:Ignorable="d"
        Title="endScreen" Height="450" Width="800">
    
    <Grid Background="Black">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="1" FontSize="15" Content="Send image to your mail" FontWeight="Bold" Click="sendMail" Margin="400,27,77,27"/>
        <Button Grid.Row="1" FontSize="15" Content="Go back to main screen" FontWeight="Bold" Click="goBack" Margin="120,27,372,27"/>

    </Grid>
</Window>
